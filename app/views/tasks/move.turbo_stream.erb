<%# Remove from old column and append to new column %>
<%= turbo_stream.remove dom_id(@task) %>
<%= turbo_stream.append "column_#{@task.column_id}_tasks",
      partial: "tasks/task_card",
      locals: { task: @task, organization: @organization, project: @project } %>